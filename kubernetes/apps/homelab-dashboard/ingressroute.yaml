apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homelab-dashboard
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`homelab.seung.site`)
      services:
        - name: homelab-dashboard
          port: 80
